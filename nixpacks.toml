[build]
providers = ["node"]

[build.node]
nodeVersion = "18"

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npm run build 2>/dev/null || true"]

[start]
cmd = "cd backend && node src/server.js"
